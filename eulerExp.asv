function [w,t] = eulerExp(f,y0,n,t0,tf)
%UNTITLED2 Summary of this function goes here
%   Detailed explanation goes here
h = (tf-t0)/n;
w(:,1) = y0;
t(1) = t0;
for k=1:n
    t(k) = (k-1)*h+t0;
    w(:,k+1) = w(:,k)+h*f(k*h+t0,w(:,k));
end

end

